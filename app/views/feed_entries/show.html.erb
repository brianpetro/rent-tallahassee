<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @feed_entry.name %>
</p>

<p>
  <%= raw @feed_entry.summary %>
</p>

<p>
  <a href="<%= @feed_entry.url %>">See More</a>
</p>

<p>
  <b>Published at:</b>
  <%= @feed_entry.published_at %>
</p>


<%= link_to 'Edit', edit_feed_entry_path(@feed_entry), class: 'btn btn-small btn-primary' %> |
<%= link_to 'Back', feed_entries_path, class: 'btn btn-small btn-primary' %>
